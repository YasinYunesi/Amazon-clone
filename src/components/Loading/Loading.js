const Loading = () => {
  return (
    <div className='loading h-screen w-screen fixed top-0 left-0 z-10 bg-black opacity-70'>
      <div class='loading' />
    </div>
  );
};

export default Loading;
